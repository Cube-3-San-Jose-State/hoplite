/**
 * Handles commands from ground control, and packages data then sends to ground control
 * 
 * Responsibilities: Listen for commands from ground control. Send commands to ground
 * control. Handle/route commands to proper location.
*/

int main(int argc, char const *argv[])
{
    /* code */
    return 0;
}
